---
import SiteLayout from '../../layouts/SiteLayout.astro';
import ServiceDetailPage from '../../components/service/ServiceDetailPage.astro';
import { fabricCollection } from '../../data/dich-vu';
import { serviceHref } from '../../lib/routes';
---

<SiteLayout
  title={fabricCollection.seo.title}
  description={fabricCollection.seo.description}
  currentPath={serviceHref(fabricCollection.slug)}
>
  <ServiceDetailPage content={fabricCollection} />
</SiteLayout>
