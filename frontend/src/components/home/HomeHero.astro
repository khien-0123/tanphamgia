---
import { homeHero } from '../../data/home';
import { routes } from '../../lib/routes';
---

<main class="relative overflow-visible bg-[#f5f0e7] p-0 sm:p-2 lg:p-0">
  <section class="relative flex flex-col overflow-visible bg-ink-dark pb-12 text-white lg:min-h-[100svh] lg:pb-0">
    <div class="absolute inset-0" aria-hidden="true">
      <video class="h-full w-full object-cover object-center" autoplay loop muted playsinline preload="metadata" poster={homeHero.poster}>
        <source src={homeHero.media} type="video/mp4" />
        Trình duyệt của bạn không hỗ trợ video nền.
      </video>
      <div class="absolute inset-0 bg-black/45"></div>
      <div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(0,0,0,.44),rgba(0,0,0,.12)_62%,rgba(6,22,35,.46))]"></div>
      <div class="absolute inset-x-0 bottom-0 h-52 bg-gradient-to-t from-black/45 to-transparent"></div>
    </div>


    <div class="relative z-10 mx-auto flex w-full max-w-[96rem] flex-col px-5 pb-8 pt-[clamp(6.5rem,12svh,7.5rem)] sm:px-8 sm:pb-10 sm:pt-[clamp(5.5rem,13svh,9rem)] lg:flex-1 lg:px-12 lg:pb-5 xl:px-16">

      <div class="flex flex-col pt-[clamp(1.5rem,4svh,3rem)] lg:flex-1 lg:pt-0">
        <div aria-hidden="true" class="hidden grow lg:block lg:grow-[4]"></div>
        <div data-reveal-stagger class="mx-auto w-full max-w-[84rem]">
          <h1 data-reveal class="mx-auto max-w-[84rem] text-center text-[clamp(2.25rem,min(1.1rem+4.2vw,8.5svh),5.75rem)] font-extrabold leading-[1.05] tracking-[-0.045em] text-white">
            {homeHero.title}
          </h1>

          <p data-reveal class="mx-auto mt-[clamp(1.25rem,3svh,1.75rem)] max-w-[75rem] text-center text-sm leading-relaxed text-white/80 sm:text-lg lg:text-xl">
            {homeHero.description}
          </p>

          <div data-reveal class="mx-auto mt-[clamp(1.5rem,3.5svh,2rem)] flex w-full max-w-full flex-row items-stretch gap-2 sm:max-w-[92%] sm:gap-2.5 md:max-w-xl">
            <a
              href={routes.lienHe}
              class="flex min-w-0 flex-1 items-center justify-center gap-2 rounded-full bg-accent-gold py-1.5 pl-4 pr-1.5 text-center text-sm font-bold leading-snug text-[#1f1b17] shadow-soft transition-all hover:scale-105 hover:shadow-gold sm:gap-2.5 sm:pl-5 sm:pr-2 sm:text-base md:text-[0.9rem]"
            >
              Nhận tư vấn giải pháp
              <span class="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#1f1b17]/15 text-[#1f1b17] sm:h-9 sm:w-9" aria-hidden="true">
                <span class="material-symbols-outlined text-base leading-none sm:text-lg">arrow_forward</span>
              </span>
            </a>
            <a
              href={routes.taiLieu}
              class="flex min-w-0 flex-1 items-center justify-center rounded-full border border-white/60 bg-white/10 px-3.5 py-3 text-center text-sm font-bold leading-snug text-white backdrop-blur-md transition-all hover:scale-105 hover:bg-white/20 sm:px-4 sm:py-3.5 sm:text-base md:px-[1.575rem] md:py-[0.7875rem] md:text-[0.9rem]"
            >
              Tải hồ sơ năng lực
            </a>
          </div>
        </div>
        <div aria-hidden="true" class="hidden h-[clamp(6rem,14svh,9rem)] lg:block"></div>
        <div aria-hidden="true" class="hidden grow lg:block"></div>
      </div>
    </div>

    {/* Dossier card — Đặt ở level section để neo sát màn hình, không bị giới hạn bởi container max-width */}
    <a
      href={homeHero.dossier.href}
      class="absolute bottom-0 right-0 z-30 hidden min-h-[4.75rem] w-[13.5rem] items-center rounded-tl-[2rem] bg-[#faf8f2] px-5 py-3 text-[#1f1b17] shadow-[-6px_-4px_16px_rgba(0,0,0,.1)] lg:flex xl:w-[14.5rem]"
    >
      <span aria-hidden="true" class="mr-4 text-xl font-bold leading-none">↗</span>
      <span class="text-left">
        <strong class="block text-base font-extrabold leading-tight tracking-[-0.025em]">{homeHero.dossier.title}</strong>
        <span class="mt-0.5 block text-sm leading-tight text-[#1f1b17]/75">{homeHero.dossier.subtitle}</span>
      </span>
    </a>
  </section>
</main>

<script src="../../scripts/hero-stats-counter.ts"></script>
