---
/**
 * @deprecated Dùng CaseStudyNarrative — giữ file để tương thích import cũ.
 */
import CaseStudyNarrative from './solution/CaseStudyNarrative.astro';

interface Props {
  challenges: string[];
  solutions: string[];
  results: string[];
}

const { challenges, solutions, results } = Astro.props;
---

<CaseStudyNarrative challenges={challenges} solutions={solutions} results={results} />
